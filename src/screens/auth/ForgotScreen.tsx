import React,{useState} from "react";import{Alert,Text,View}from"react-native";import{useAuth}from"../../context/AuthContext";import{Button,Field,styles}from"../../components/UI";
export default function Forgot(){const{reset}=useAuth();const[e,setE]=useState("");return <View style={{flex:1,padding:24,justifyContent:"center"}}><Text style={styles.title}>Reset password</Text><Field placeholder="Email" value={e} onChangeText={setE}/><Button title="Send reset email" onPress={async()=>{try{await reset(e);Alert.alert("Done","Check your email.")}catch(x:any){Alert.alert("Error",x.message)}}}/></View>}